# A skelton for Chrome packaged apps

## Using

- gulp
- coffeescript
- sass
- slim
- bower
- browserify

## Usage
Gulp builds sources in the `source` directory into the `build` directry.

```sh
$ gulp watch
```

Then, develop your app in the `source` directory.
